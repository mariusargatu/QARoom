\* TLC configuration for Dedup.tla (verifiable-invariants Phase 3, ADR-0024; T19).
\* The committed-state twin of packages/messaging/src/dedup.ts (alreadyProcessed / markProcessed).
SPECIFICATION Spec

\* Once the broker's redelivery budget is spent the event is settled (recorded, effect applied
\* once) and has no successor — a legitimate end, not a deadlock. EventuallyProcessed proves the
\* event is recorded before then.
CHECK_DEADLOCK FALSE

CONSTANT MaxDeliveries = 4

INVARIANT TypeOK
INVARIANT NoDoubleApply
INVARIANT RecordedIffApplied

PROPERTY EventuallyProcessed
