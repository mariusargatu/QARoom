\* TLC configuration for Rollout.tla (verifiable-invariants Phase 3, ADR-0024; T19).
\* The committed-state twin of packages/contracts/src/machines/rollout.machine.ts.
SPECIFICATION Spec

\* Deadlock checking stays ON: the rollout machine is non-terminating by design — every state
\* has an outgoing edge — so a TLC deadlock would be a real "dead-end state" bug, not a feature.

INVARIANT TypeOK
INVARIANT EnabledImpliesCanary

PROPERTY EventuallySettles
