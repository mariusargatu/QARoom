apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "qaroom-service.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "qaroom-service.labels" . | nindent 4 }}
  annotations:
    # No registry backs these images — kind load pushes a freshly built local image straight into
    # the cluster, so there is nothing to pull "Always" and no digest to pin (CKV_K8S_14/15/43).
    checkov.io/skip1: CKV_K8S_14=local kind-loaded image, no registry to tag beyond `latest`
    checkov.io/skip2: CKV_K8S_15=local kind-loaded image, IfNotPresent/Never by design (no registry to pull Always from)
    checkov.io/skip3: CKV_K8S_43=local kind-loaded image, no registry to resolve a digest against
    # Not enabled: unverified across all five services without a live cluster check — moderator-
    # agent's transformers/tokenizers stack in particular needs a writable HF cache dir that no
    # image declares yet. Tracked as a real gap, not silently accepted (CKV_K8S_22).
    checkov.io/skip4: CKV_K8S_22=no writable-cache volume plumbed for moderator-agent's ML stack yet
spec:
  replicas: {{ .Values.replicaCount }}
  # Zero-unavailability rolling update (T13). maxUnavailable:0 keeps the desired replica count up
  # for the whole rollout — the N+1 pod must be Ready before an N pod is torn down — so there is a
  # real N/N+1 coexistence window: v1 and v2 serve REST and consume NATS at the same time. The
  # runtime coexistence test (packages/contracts/src/events/mixed-version-coexistence.test.ts)
  # proves an N pod survives that window (parses the N+1 producer's events, quarantines a version it
  # cannot understand instead of crash-looping). maxSurge:1 bounds the extra pod to one at a time.
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      {{- include "qaroom-service.selectorLabels" . | nindent 6 }}
      qaroom.io/component: app
  template:
    metadata:
      labels:
        {{- include "qaroom-service.selectorLabels" . | nindent 8 }}
        qaroom.io/component: app
      {{- if .Values.metrics.enabled }}
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/path: {{ .Values.metrics.path | quote }}
        prometheus.io/port: {{ .Values.container.port | quote }}
      {{- end }}
    spec:
      serviceAccountName: {{ include "qaroom-service.serviceAccountName" . }}
      # No service calls the K8s API — nothing needs its own token mounted (CKV_K8S_38).
      automountServiceAccountToken: false
      securityContext:
        {{- include "qaroom-service.podSecurityContext" . | nindent 8 }}
      containers:
        - name: {{ include "qaroom-service.name" . }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          securityContext:
            {{- include "qaroom-service.containerSecurityContext" . | nindent 12 }}
          ports:
            - name: http
              containerPort: {{ .Values.container.port }}
          env:
            - name: PORT
              value: {{ .Values.container.port | quote }}
            - name: NODE_ENV
              value: {{ .Values.nodeEnv | quote }}
            {{- $dburl := include "qaroom-service.databaseUrl" . }}
            {{- if $dburl }}
            - name: DATABASE_URL
              value: {{ $dburl | quote }}
            {{- end }}
            {{- if .Values.otel.enabled }}
            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              value: {{ .Values.otel.endpoint | quote }}
            - name: OTEL_EXPORTER_OTLP_PROTOCOL
              value: {{ .Values.otel.protocol | quote }}
            - name: OTEL_SERVICE_NAME
              value: {{ .Values.otel.serviceName | default (include "qaroom-service.name" .) | quote }}
            - name: OTEL_RESOURCE_ATTRIBUTES
              value: {{ .Values.otel.resourceAttributes | quote }}
            - name: OTEL_SEMCONV_STABILITY_OPT_IN
              value: {{ .Values.otel.semconvOptIn | quote }}
            {{- end }}
            {{- range $k, $v := .Values.extraEnv }}
            - name: {{ $k }}
              value: {{ $v | quote }}
            {{- end }}
          livenessProbe:
            httpGet:
              path: {{ .Values.probes.liveness.path }}
              port: {{ .Values.container.port }}
            initialDelaySeconds: {{ .Values.probes.liveness.initialDelaySeconds }}
            periodSeconds: {{ .Values.probes.liveness.periodSeconds }}
            failureThreshold: {{ .Values.probes.liveness.failureThreshold }}
          readinessProbe:
            httpGet:
              path: {{ .Values.probes.readiness.path }}
              port: {{ .Values.container.port }}
            initialDelaySeconds: {{ .Values.probes.readiness.initialDelaySeconds }}
            periodSeconds: {{ .Values.probes.readiness.periodSeconds }}
            failureThreshold: {{ .Values.probes.readiness.failureThreshold }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
