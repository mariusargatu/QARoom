{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "lib": ["ES2023", "DOM", "DOM.Iterable"],
    "jsx": "react-jsx",
    "types": ["vite/client", "node"]
  },
  "include": ["src", "tests", ".storybook", "playwright.config.ts", "vite.config.ts"],
  "//": "vitest.*.config.ts and their setups are excluded from app typecheck (run by Vitest's own esbuild loader, not tsc); the browser-test files under src/ ARE typechecked."
}
