{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "lib": ["ES2023", "DOM", "DOM.Iterable"],
    "jsx": "react-jsx",
    "types": ["vite/client", "node"]
  },
  "include": ["src", "tests", ".storybook", "playwright.config.ts", "vite.config.ts"],
  "//": "playwright-ct.config.ts is excluded from app typecheck: it merges vite-7 plugins into the vite bundled by the pinned @playwright/experimental-ct-react@1.60, whose Plugin types differ. Playwright executes the config via its own loader (esbuild), not tsc."
}
