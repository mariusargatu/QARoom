{
  "$schema": "./node_modules/@stryker-mutator/core/schema/stryker-schema.json",
  "governance_comment": "Critical-modules list is GOVERNED in docs/03-testing-strategy.md §11 — adding a module is an ADR, removing requires a retrospective. ticket-store.ts (WS-ticket one-use + 30s expiry) added in ADR-0016's 2026-06-19 addendum.",
  "packageManager": "pnpm",
  "testRunner": "vitest",
  "plugins": ["@stryker-mutator/vitest-runner"],
  "vitest": { "configFile": "vitest.stryker.config.ts" },
  "mutate": ["src/ticket-store.ts"],
  "ignoreStatic": true,
  "inPlace": true,
  "tempDirName": ".stryker-tmp",
  "reporters": ["json", "clear-text"],
  "jsonReporter": { "fileName": "../../test-results/stryker-identity.json" },
  "thresholds": { "high": 90, "low": 80, "break": 80 }
}
