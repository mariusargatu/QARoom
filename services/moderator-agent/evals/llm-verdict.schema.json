{
  "additionalProperties": false,
  "description": "The structured output the LLM MUST return (OpenAI structured outputs / ``response_format``).\n\nCitation-bearing (ADR-0020): the model draws ``disposition`` + ``rationale`` from the RETRIEVED\npolicy context and names the ``cited_rules`` / ``precedents`` it relied on. The citation arrays\ndefault empty so a clean ``approve`` (or a minimal test double) need not enumerate them; the\nself-check node validates them against what was actually retrieved before recording.",
  "properties": {
    "disposition": {
      "enum": [
        "approve",
        "remove",
        "escalate_to_human"
      ],
      "title": "Disposition",
      "type": "string"
    },
    "cited_rules": {
      "items": {
        "maxLength": 100,
        "type": "string"
      },
      "maxItems": 16,
      "title": "Cited Rules",
      "type": "array"
    },
    "precedents": {
      "items": {
        "maxLength": 2000,
        "pattern": "^[^\\x00]*$",
        "type": "string"
      },
      "maxItems": 16,
      "title": "Precedents",
      "type": "array"
    },
    "departs_from_precedent": {
      "default": false,
      "title": "Departs From Precedent",
      "type": "boolean"
    },
    "rationale": {
      "maxLength": 4000,
      "pattern": "^[^\\x00]*$",
      "title": "Rationale",
      "type": "string"
    },
    "confidence": {
      "maximum": 1,
      "minimum": 0,
      "title": "Confidence",
      "type": "number"
    }
  },
  "required": [
    "disposition",
    "rationale",
    "confidence"
  ],
  "title": "LlmVerdict",
  "type": "object"
}
