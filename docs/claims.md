# Falsifiable claims, with live verdicts

> Generated by `pnpm claims:render`; do not edit by hand. 8 falsifiable claims across 5 of the 12 documented boundaries. 3 verified (5 stale until summary.json is regenerated in CI).

A VERIFIED claim resolved against the evidence below; a STALE claim's runner is absent from the
current snapshot and resolves on the next full run. A claim that cannot go red when its toggle is
set fails `pnpm claims:verify` outright: that is the gate, not this page.

| Status | Claim | Boundary | Breaks when | Evidence | Falsify |
|---|---|---|---|---|---|
| VERIFIED | A webhook signature binds the timestamp, so a captured (body, signature) pair cannot be replayed. | `delivery-edge` | `CHAOS_WEBHOOK_SIGN_BODY_ONLY` | runner `@qaroom/webhooks`: passed=78 (suite-level) | `pnpm prove webhook-signing --break` |
| VERIFIED | Every webhook delivery reaches a terminal state; a failed send is retried, never silently dropped. | `delivery-edge` | `CHAOS_WEBHOOK_DROP_ON_FAIL` | runner `@qaroom/webhooks`: passed=78 (suite-level) | `pnpm prove webhook-at-least-once --break` |
| STALE | The moderator escalates to a human on a low-confidence verdict instead of guessing (FR5 calibration). | `external-dep` | `MODERATOR_DISABLE_ABSTAIN` | unresolved | `pnpm prove moderator-abstain --break` |
| STALE | The moderator never confidently approves content the precedent flags: an approve that diverges from majority-remove precedent escalates to a human instead. The safety invariant, not gold-set agreement, is the bar — which is what lets the cheaper gpt-5-nano draft model stay safe (a confident-but-wrong approve is caught structurally, not shipped). | `external-dep` | `MODERATOR_DISABLE_APPROVE_GUARD` | unresolved | `pnpm prove moderator-no-confident-approve-of-flag --break` |
| STALE | The moderator fences attacker-controlled post bodies as DATA before they reach the model; disabling the guard leaves them in instruction context. | `external-dep` | `MODERATOR_DISABLE_INPUT_GUARD` | unresolved | `pnpm prove input-guard-fences-untrusted-body --break` |
| VERIFIED | A community's feed contains exactly its own posts and never another tenant's, even under an arbitrary interleave of cross-community writes (Commitment 9). | `tenancy` | `CONTENT_BUG_TENANT_LEAK` | runner `@qaroom/content`: passed=26 (suite-level) | `pnpm prove tenant-isolation --break` |
| STALE | Every span the deployed system emits carries tenant.id (Commitment 9); a dropped stamp is caught by the live Jaeger audit. | `observability` | `CHAOS_TENANT_SPAN_DROP` | unresolved | `pnpm prove tenant-span-everywhere --break` |
| STALE | The transactional outbox keeps mutating HTTP latency independent of the broker: publishing on the request path breaches the vote SLO even on a healthy broker. | `process-async` | `CHAOS_SYNC_PUBLISH` | unresolved | `pnpm prove outbox-isolates-broker-latency --break` |

Derived from summary.json, commit 59018c6, generated 2026-06-13T16:28:35.057Z. An unresolved claim renders STALE, never a faked green.
