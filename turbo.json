{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"]
    },
    "typecheck": {
      "dependsOn": ["^typecheck"]
    },
    "openapi:generate": {
      "outputs": ["openapi.yaml", "src/**/openapi.yaml"]
    },
    "asyncapi:generate": {
      "outputs": ["asyncapi.yaml", "src/**/asyncapi.yaml"]
    },
    "test": {
      "outputs": ["test-results/**", "coverage/**"],
      "env": ["VITEST_SEED", "QAROOM_TEST_DATABASE_URL", "OPENAI_API_KEY"]
    }
  }
}
